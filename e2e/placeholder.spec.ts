import { test } from '@playwright/test';

test.describe('Placeholder', () => {
  test('no e2e tests for this project', async () => {
    // Intentionally empty: UI e2e tests are not applicable here.
  });
});
